enum PinCodeType { register, changeEmail, changeUsername, changePassword }

enum PlayCompleteStatus { success, normal, bad }

enum MessageType { followMessage, defaultMessage }

enum MessageRoute { home, profile }
